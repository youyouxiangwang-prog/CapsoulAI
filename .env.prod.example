# Production Environment Configuration Template
# Copy this file to .env.prod and modify according to your setup
# cp .env.prod.example .env.prod

# Application Configuration
APP_NAME="CapsoulAI API"
APP_VERSION="1.0.0"
DEBUG=false
ENVIRONMENT=production
SECRET_KEY=your-super-secret-key-change-this-in-production

# Server Configuration - Please modify according to your setup
# Set to your server's public IP address (e.g., 13.60.254.11)
SERVER_IP=YOUR_SERVER_IP_HERE
# Use IP address as domain for IP-only access
DOMAIN=YOUR_SERVER_IP_HERE
# HTTP access (no HTTPS for IP-only setup)
ENABLE_HTTPS=false

# Service Port Configuration
FRONTEND_PORT=80
BACKEND_PORT=8000
NGINX_HTTP_PORT=80
# HTTPS port disabled for HTTP-only setup
# NGINX_HTTPS_PORT=443

# API Path Configuration
API_PREFIX=/api/v1

# Production CORS Configuration
# Will be automatically updated during deployment based on SERVER_IP
BACKEND_CORS_ORIGINS='["http://YOUR_SERVER_IP_HERE","http://127.0.0.1"]'

# Database Configuration
POSTGRES_DB=capsoulai_prod
POSTGRES_USER=capsoulai_prod_user
POSTGRES_PASSWORD=your-strong-database-password-here

# Redis Configuration
REDIS_PASSWORD=your-strong-redis-password-here

# Celery Configuration
CELERY_BROKER_URL=redis://:your-strong-redis-password-here@redis:6379/0
CELERY_RESULT_BACKEND=redis://:your-strong-redis-password-here@redis:6379/0
CELERY_WORKER_NAME=prod-worker

# File Storage
RECORDINGS_DIR=/app/uploads

# AWS Configuration (if using ECR)
AWS_ECR_REGISTRY=your-account-id.dkr.ecr.us-east-1.amazonaws.com
AWS_REGION=us-east-1

# SSL Configuration - Disabled for HTTP-only setup
# If SSL is needed in the future, uncomment and configure these
# SSL_CERT_PATH=/app/ssl/cert.pem
# SSL_KEY_PATH=/app/ssl/key.pem

# Other Configuration
PROJECT_NAME=CapsoulAI
MAX_FILE_SIZE=10485760
UPLOAD_FOLDER=uploads
